general:
    artifacts:
        - /Users/b.gogia/AndroidStudioProjects/ConstraintLayout/app/build/outputs/apk

machine:
    environment:
        ANDROID_HOME: /Users/b.gogia/AppData/Local/android/Sdk

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-25.0.0,android-25,extra-google-m2repository,extra-google-google_play_services,extra-android-support
        - chmod +x gradlew
        - ANDROID_HOME=/Users/b.gogia/AppData/Local/android/Sdk ./gradlew dependencies

test:
    override:
        - (./gradlew assemble):
            timeout: 360